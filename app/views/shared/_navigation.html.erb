<!-- Navigation -->
<nav id="mainNav" class="navbar navbar-default navbar-fixed-top navbar-custom">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header page-scroll">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span> Menu <i class="fa fa-bars"></i>
            </button>
            <% if controller_name == "welcome" %>
              <a class="navbar-brand" href="#page-top">DingtuBike</a>
            <% else %>
              <%= link_to :DingtuBike , root_path , class:"navbar-brand" %>
            <% end %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
              <% if controller_name == "welcome" %>
                <li class="hidden">
                    <a href="#page-top"></a>
                </li>
                <li class="page-scroll">
                    <a href="#hot-bikes">Bike</a>
                </li>
                <li class="page-scroll">
                    <a href="#about">About</a>
                </li>
                <li class="page-scroll">
                    <a href="#contact">Contact</a>
                </li>
              <% end %>
                <li class="user page-scroll">
                    <% if !current_user %>
                      <ul class="nav navbar-nav navbar-right">
                        <li><%= link_to '<span class="fa fa-user"></span> Sign Up'.html_safe, user_registration_path %></li>
                        <li><%= link_to '<span class="fa fa-sign-in"></span> Login'.html_safe, new_user_session_path %></li>
                      </ul>
                    <% else %>
                      <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                        <%= current_user.email %>
                        <span class="caret"></span>
                      </a>
                      <ul class="dropdown-menu" role="menu">
                        <li><%= link_to "logout", destroy_user_session_path , method: :delete %></li>
                      </ul>
                    <% end %>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
